# คู่มือการตั้งค่า LINE Bot Webhook

## 🎯 ฟีเจอร์ใหม่: ตรวจสอบข้อมูลส่วนตัว

ผู้ใช้สามารถพิมพ์ **"personal"** ในแชท LINE Bot เพื่อดูข้อมูลการลงทะเบียนของตัวเอง

---

## 📋 ขั้นตอนการตั้งค่า

### 1. เอา Channel Access Token และ Channel Secret

1. ไปที่ [LINE Developers Console](https://developers.line.biz/console/)
2. เลือก Channel ที่สร้างไว้
3. ไปที่แท็บ **"Messaging API"**
4. คัดลอก:
   - **Channel access token** (กดปุ่ม Issue ถ้ายังไม่มี)
   - **Channel secret** (อยู่ในแท็บ "Basic settings")

### 2. เพิ่มค่าใน .env

แก้ไขไฟล์ `.env` ใส่ค่าที่ได้:

```env
MONGO_URI=mongodb+srv://niceppn:XCFyloY5PT1DSfOb@cluster0.ljo0j.mongodb.net/linebot_register?retryWrites=true&w=majority

LINE_CHANNEL_ACCESS_TOKEN=YOUR_CHANNEL_ACCESS_TOKEN_HERE
LINE_CHANNEL_SECRET=YOUR_CHANNEL_SECRET_HERE
```

### 3. ตั้งค่า Webhook URL

1. ในหน้า **"Messaging API"**
2. หาส่วน **"Webhook settings"**
3. กดปุ่ม **"Edit"**
4. ใส่ Webhook URL:
   ```
   https://bcd5a8c9da90.ngrok-free.app/webhook
   ```
5. เปิด **"Use webhook"** = ON
6. กดปุ่ม **"Verify"** เพื่อทดสอบ (ควรได้ Success)

### 4. ปิด Auto-reply (ถ้าไม่ต้องการ)

1. ไปที่แท็บ **"Messaging API"**
2. หาส่วน **"Auto-reply messages"**
3. กดไปที่ **"LINE Official Account Manager"**
4. ปิด Auto-reply messages

### 5. รีสตาร์ทเซิร์ฟเวอร์

```bash
# หยุดเซิร์ฟเวอร์เดิม (Ctrl+C)
# รันใหม่
python server.py
```

---

## 🧪 วิธีทดสอบ

### 1. เพิ่ม Bot เป็นเพื่อน
1. ไปที่แท็บ **"Messaging API"**
2. สแกน QR Code หรือคลิก **"Add friend"**
3. เพิ่ม Official Account เป็นเพื่อน

### 2. ลงทะเบียนผ่าน LIFF
1. เปิด LIFF URL ใน LINE: `https://liff.line.me/YOUR_LIFF_ID`
2. กรอกข้อมูลและส่ง
3. ระบบจะบันทึก `lineUserId` ไว้

### 3. ทดสอบคำสั่ง "personal"
1. เปิดแชทกับ Bot
2. พิมพ์: **personal**
3. Bot จะตอบกลับด้วยข้อมูลการลงทะเบียนของคุณ

---

## 📱 ตัวอย่างการตอบกลับ

### กรณีมีข้อมูล:
```
📋 ข้อมูลการลงทะเบียนของคุณ
═══════════════════════
จำนวนรายการ: 1 รายการ

【 รายการที่ 1 】
👤 ชื่อ: นาย สมชาย ใจดี
🏢 หน่วยงาน: แผนกไอที (001)
🆔 รหัสพนักงาน: E001
📱 เบอร์: 0812345678
💬 LINE: สมชาย ใจดี
📅 ลงทะเบียนเมื่อ: 06/11/2025 12:00
✅ สถานะ: active
```

### กรณีไม่มีข้อมูล:
```
❌ ไม่พบข้อมูลการลงทะเบียนของคุณในระบบ

กรุณาลงทะเบียนก่อนใช้งาน
```

---

## 🔧 Webhook Endpoint

**URL**: `/webhook`  
**Method**: `POST`  
**Content-Type**: `application/json`

### Request Body (จาก LINE):
```json
{
  "events": [
    {
      "type": "message",
      "message": {
        "type": "text",
        "text": "personal"
      },
      "source": {
        "userId": "U1234567890abcdefghijklmnopqrstuv"
      },
      "replyToken": "xxxxx"
    }
  ]
}
```

### Response:
```json
{
  "status": "ok"
}
```

---

## 🛡️ ความปลอดภัย

- ✅ ตรวจสอบ **X-Line-Signature** เพื่อยืนยันว่าคำขอมาจาก LINE จริงๆ
- ✅ ใช้ **Channel Secret** ในการ verify signature
- ✅ ส่งข้อมูลเฉพาะของผู้ใช้เอง (กรองด้วย `lineUserId`)

---

## ⚠️ หมายเหตุ

1. **ngrok URL** - ถ้า restart ngrok URL จะเปลี่ยน ต้องอัพเดท Webhook URL ใหม่
2. **Channel Access Token** - มีอายุนาน เก็บไว้ในไฟล์ `.env` อย่างปลอดภัย
3. **Rate Limit** - LINE API มี rate limit อย่าส่งข้อความมากเกินไป

---

## 🎉 เสร็จสิ้น!

ตอนนี้ระบบพร้อมใช้งานแล้ว:
- ✅ ลงทะเบียนผ่าน LIFF
- ✅ พิมพ์ "personal" เพื่อดูข้อมูลตัวเอง
- ✅ ป้องกันการลงทะเบียนซ้ำด้วย LINE User ID
