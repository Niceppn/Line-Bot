# คู่มือการตั้งค่า LIFF (LINE Front-end Framework)

## 🎯 ขั้นตอนการตั้งค่า LIFF

### 1. สร้าง LINE Developers Account และ Channel

1. ไปที่ [LINE Developers Console](https://developers.line.biz/console/)
2. ล็อกอินด้วยบัญชี LINE ของคุณ
3. สร้าง Provider (ถ้ายังไม่มี):
   - คลิก "Create a new provider"
   - ใส่ชื่อ Provider (เช่น "บริษัทโอวาทเมท")

4. สร้าง Channel:
   - เลือก Provider ที่สร้างไว้
   - คลิก "Create a Messaging API channel"
   - กรอกข้อมูล:
     - **Channel name**: LINE Bot Register
     - **Channel description**: ระบบลงทะเบียนพนักงาน
     - **Category**: เลือกตามความเหมาะสม
     - **Subcategory**: เลือกตามความเหมาะสม
   - ยอมรับเงื่อนไข และคลิก "Create"

### 2. เพิ่ม LIFF App

1. ไปที่แท็บ **"LIFF"** ในหน้า Channel ของคุณ
2. คลิก **"Add"** เพื่อสร้าง LIFF app ใหม่

3. กรอกข้อมูล LIFF:
   ```
   LIFF app name: LINE Bot Register Form
   Size: Full
   Endpoint URL: https://bcd5a8c9da90.ngrok-free.app/
   Scope: profile (✓)
   Bot link feature: On (Aggressive)
   Scan QR: Off
   Module Mode: Off
   ```

4. คลิก **"Add"**

5. คัดลอก **LIFF ID** ที่ได้ (รูปแบบ: `1234567890-abcdefgh`)

### 3. อัพเดท LIFF ID ในโค้ด

แก้ไขไฟล์ `register-form.html` บรรทัดที่มี:
```javascript
const LIFF_ID = 'YOUR_LIFF_ID';
```

เปลี่ยนเป็น:
```javascript
const LIFF_ID = '1234567890-abcdefgh'; // ใส่ LIFF ID ที่ได้จากขั้นตอนที่ 2
```

### 4. ทดสอบ LIFF

#### วิธีที่ 1: ผ่าน LINE Official Account
1. ไปที่แท็บ **"Messaging API"** ในหน้า Channel
2. สร้าง Rich Menu หรือ Auto-reply message ที่มี URL:
   ```
   https://liff.line.me/YOUR_LIFF_ID
   ```
   (แทน YOUR_LIFF_ID ด้วย LIFF ID ที่ได้)

3. เพิ่ม Official Account เป็นเพื่อนใน LINE
4. คลิกที่ Rich Menu หรือ Reply message เพื่อเปิดฟอร์ม

#### วิธีที่ 2: ทดสอบด้วย LINE App (แชร์ลิงก์)
1. แชร์ลิงก์นี้ในแชท LINE:
   ```
   https://liff.line.me/YOUR_LIFF_ID
   ```
2. คลิกที่ลิงก์เพื่อเปิดฟอร์ม

## 📋 ข้อมูลที่ LIFF จะดึงมา

เมื่อเปิดฟอร์มผ่าน LIFF จะดึงข้อมูลต่อไปนี้อัตโนมัติ:

- ✅ **LINE User ID**: รหัสเฉพาะของผู้ใช้ (ไม่ซ้ำกัน)
- ✅ **LINE Display Name**: ชื่อที่แสดงใน LINE
- ✅ **LINE Profile Picture**: URL รูปโปรไฟล์ (ถ้าต้องการเพิ่ม)

ข้อมูลเหล่านี้จะถูกบันทึกลงใน MongoDB พร้อมกับข้อมูลอื่นๆ ที่กรอกในฟอร์ม

## 🔧 การตั้งค่าเพิ่มเติม

### เปลี่ยน ngrok URL
ถ้า ngrok URL เปลี่ยน:
1. อัพเดท Endpoint URL ใน LIFF settings
2. อัพเดท API URL ในไฟล์ `register-form.html`:
   ```javascript
   fetch('https://YOUR_NEW_NGROK_URL/api/register', {
   ```

### Whitelist Domain (ถ้าใช้ Custom Domain)
ถ้าใช้ domain ของตัวเอง:
1. ไปที่แท็บ **"LIFF"**
2. เพิ่ม domain ใน **"Whitelisted domains"**

## 🐛 แก้ไขปัญหา

### LIFF ไม่ทำงาน
- ตรวจสอบว่า LIFF ID ถูกต้อง
- ตรวจสอบว่า ngrok ยังทำงานอยู่
- ตรวจสอบ Console ใน Browser Developer Tools

### ไม่สามารถดึงข้อมูล Profile
- ตรวจสอบว่าเปิด Scope "profile" ใน LIFF settings
- ลองล็อกเอาต์และล็อกอินใหม่

### ทดสอบนอก LINE App
- ฟอร์มจะทำงานแบบปกติ (ไม่มี LIFF)
- ต้องกรอก LINE ID เอง

## 📱 LIFF URL Format

```
https://liff.line.me/{LIFF_ID}
```

ตัวอย่าง:
```
https://liff.line.me/1234567890-abcdefgh
```

## 🔒 ความปลอดภัย

- LIFF จะทำงานเฉพาะใน LINE App เท่านั้น
- LINE User ID เป็นรหัสเฉพาะที่ LINE สร้างให้
- ไม่สามารถปลอมแปลงข้อมูล Profile ได้

## 📊 ข้อมูลที่บันทึกใน MongoDB

```json
{
  "deptCode": "001",
  "deptName": "แผนกไอที",
  "empCode": "E001",
  "prefix": "นาย",
  "firstName": "สมชาย",
  "lastName": "ใจดี",
  "mobile": "0812345678",
  "lineId": "สมชาย ใจดี",
  "lineUserId": "U1234567890abcdefghijklmnopqrstuv",
  "lineDisplayName": "สมชาย ใจดี",
  "createdAt": "2025-11-06T12:00:00.000Z",
  "status": "active"
}
```

## 🎉 เสร็จสิ้น

หลังจากตั้งค่าเรียบร้อย:
1. แชร์ LIFF URL ให้พนักงาน
2. พนักงานเปิดลิงก์ใน LINE
3. ระบบจะดึงข้อมูล LINE อัตโนมัติ
4. กรอกข้อมูลที่เหลือและส่ง
5. ข้อมูลจะถูกบันทึกใน MongoDB Atlas
